#include "board.h"

typedef struct player{
	char  Name[255];
	//enum Field Color = EMPTY;
	
} player;

void setPlayerName(player * thisP, const  char * name);
//void setPlayerColor(player * thisP, const Field *color);
